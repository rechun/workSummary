

```javascript
function toCurry(func,...args){
  return function(){
    args=[...args,...arguments];//合并上一次缓存的参数 和本次传入参数
    if(args.length<func.length){//判断参数数量是否足够
      return toCurry(func,...args);//递归 形成新闭包
    }else{
      return func.apply(null,args);//参数满足 执行函数并返回结果
    }
  }
}
```
