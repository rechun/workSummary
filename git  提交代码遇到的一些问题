1.当我push origin branch 时候遇到 Updates were rejected because the remote contains work that you do

   原因：在本地新建库后，与远程仓库的内容不一致导致的。
   
   解决方法：在向远程库推送的时候，要先进行pull，让本地新建的库和远程库进行同步。
   
   
正确步骤：

git init //初始化仓库
git add .(文件name) //添加文件到本地仓库
git commit -m “first commit” //添加文件描述信息
git remote add origin + 远程仓库地址 //链接远程仓库，创建主分支
git pull origin master // 把本地仓库的变化连接到远程仓库主分支
git push -u origin master //把本地仓库的文件推送到远程仓库


2.push origin 遇到 Please enter a commit message to explain why this merge is necessary,

基本原因是因为本地库的代码跟远程库有冲突需要合并,处理方法

按键盘字母 i 进入insert(输入)模式,编辑必要的注释 说明合并的原因,编辑的内容会在第一行显示,也可不修改

按"Esc"

输入":wq",按回车键即可


1、查看本地分支 ： git branch

2 、删除本地分支： git branch -d [branchname] 

3、删除远程分支: git push origin --delete [branchname] 

4、创建分支 ：git branch [branchname] 

5、 查看全部分支(包含本地和远程) ：git branch -a

6、根据指定版本号创建分支: git checkout -b branchName commitId

链接内网 ssh root@xx.xx.xx.xxx

输入密码

ls查看路径

删除远程分支遇到 
遇到remote ref does not exist   
 是因为加了origin/xxx  只需要输入分支名就可以

